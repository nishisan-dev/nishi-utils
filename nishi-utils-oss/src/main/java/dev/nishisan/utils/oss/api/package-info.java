/**
 * Tipos públicos e enums compartilhados pela API do ngrrd.
 *
 * <p>Estes tipos aparecem tanto na definição YAML (deserialização Jackson) quanto
 * no runtime das operações de escrita e leitura.</p>
 */
package dev.nishisan.utils.oss.api;
